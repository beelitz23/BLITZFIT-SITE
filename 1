import { useState } from 'react';
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

export default function BlitzFitHome() {
  const [email, setEmail] = useState('');
  const [form, setForm] = useState({ name: '', message: '', email: '' });

  return (
    <div className="min-h-screen bg-white text-black">
      <header className="bg-black text-white p-6 text-center text-3xl font-bold">
        BLITZFIT
      </header>

      <main className="p-6 space-y-12">
        <section className="text-center">
          <h2 className="text-4xl font-semibold mb-2">Welcome to BLITZFIT</h2>
          <p className="text-lg text-gray-600">Transform your body. Elevate your life.</p>
        </section>

        <section id="about" className="max-w-3xl mx-auto text-center">
          <h3 className="text-2xl font-semibold mb-4">About Us</h3>
          <p className="text-gray-700">BLITZFIT is your modern solution to fitness. With expert coaching and tailor-made programs, we help you achieve peak physical health.</p>
        </section>

        <section id="services" className="max-w-4xl mx-auto">
          <h3 className="text-2xl font-semibold text-center mb-4">Our Services</h3>
          <ul className="grid md:grid-cols-3 gap-4 text-center">
            <li className="bg-gray-100 p-4 rounded-xl shadow">Personal Training</li>
            <li className="bg-gray-100 p-4 rounded-xl shadow">Group Fitness Classes</li>
            <li className="bg-gray-100 p-4 rounded-xl shadow">Nutrition Guidance</li>
          </ul>
        </section>

        <section id="courses" className="max-w-4xl mx-auto">
          <h3 className="text-2xl font-semibold text-center mb-4">Courses</h3>
          <ul className="grid md:grid-cols-2 gap-4 text-center">
            <li className="bg-blue-50 p-4 rounded-xl shadow">8-Week Shred Program</li>
            <li className="bg-blue-50 p-4 rounded-xl shadow">Strength & Conditioning Plan</li>
          </ul>
        </section>

        <section id="contact" className="max-w-2xl mx-auto">
          <h3 className="text-2xl font-semibold text-center mb-4">Contact Us</h3>
          <form className="space-y-4">
            <Input placeholder="Your Name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} />
            <Input placeholder="Your Email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} />
            <Textarea placeholder="Your Message" value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} />
            <Button type="submit">Send Message</Button>
          </form>
        </section>

        <section id="newsletter" className="text-center mt-12">
          <h3 className="text-2xl font-semibold mb-2">Subscribe to Our Newsletter</h3>
          <div className="flex justify-center gap-2">
            <Input placeholder="Enter your email" value={email} onChange={(e) => setEmail(e.target.value)} className="max-w-sm" />
            <Button>Subscribe</Button>
          </div>
        </section>
      </main>

      <footer className="bg-gray-900 text-white p-4 text-center mt-12">
        &copy; {new Date().getFullYear()} BLITZFIT. All rights reserved.
      </footer>
    </div>
  );
}
